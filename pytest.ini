[pytest]
markers =
    integration: marks tests that require external dependencies (like llama.cpp) and should be run separately
    optional: marks tests that require optional dependencies (like CLIP/PIL) and can be skipped when missing
asyncio_mode = auto
addopts = -m "not integration"
pythonpath = src
